# FastenerModel AI — FreeCAD 跨平台重构总结

> 标准紧固件智能建模系统 · ZWCAD（中望CAD）→ FreeCAD 跨平台改造

---

## 一、项目背景

原项目 **「AI+CAD demo」**（`/Users/kexinlv/Downloads/AI+CAD demo`）是「航天发动机标准紧固件智能建模系统」（FastenerModel AI），核心链路：

```
国标标准文档 → LLM 智能解析参数 → three.js 3D 预览 → CAD 建模 + 属性导出
```

**痛点**：原 CAD 建模层依赖中望CAD（ZWCAD）的 COM/ActiveX 自动化（`pywin32`）和 AutoLISP，两者都只能在 Windows 上运行，无法在 macOS 上重构。

## 二、重构目标

1. 用 **FreeCAD Python API** 替换 ZWCAD COM/ActiveX 层，实现 macOS / Windows / Linux 跨平台；
2. **新建独立项目**（`/Users/kexinlv/Downloads/AI+CAD-FreeCAD`），原项目 100% 保留不动；
3. Web 前端、three.js 预览、LLM 文档解析等跨平台部分保持不变。

## 三、改动清单（旧 → 新）

| 原（ZWCAD，仅 Windows） | 新（FreeCAD，跨平台） |
|---|---|
| `scripts/zwcad_modeler.py`（pywin32/COM） | `scripts/freecad_modeler.py`（Part 模块） |
| `zwcad_fastener_model修改.py`（蝶形螺母） | `freecad_fastener_model.py` |
| `scripts/zwcad_fastener.lsp`（AutoLISP） | 移除（FreeCAD 宏即 Python） |
| `src/app/api/export-zwcad/` | `src/app/api/export-freecad/` |
| AttributeTable「中望CAD」导出区 | 「FreeCAD 建模」导出区 |
| `scripts/dev.sh`（Linux `ss`） | 改用 macOS 通用的 `lsof` |
| `scripts/requirements.txt`（pywin32） | 移除 pywin32，注明 FreeCAD 为应用自带 |
| `generate-library.js` → `zwcad_batch_model.py` | → `freecad_batch_model.py` |
| 输出 `.dwg` | 输出 `.step` + `.stl`（可选 `.FCStd`） |

**新增文件**：`scripts/run_freecad.sh`（定位 freecadcmd）、`README.md`、`.env.example`。

## 四、已实现功能

### 1. Web 端（跨平台，原样保留）
- 文档输入：粘贴国标文本 / 上传 PDF、TXT、DOCX
- AI 解析：豆包（Coze）/ 千问 / DeepSeek / Kimi / GLM / MiniMax 多模型路由
- 3D 预览：three.js 参数化渲染 7 类紧固件，拖拽旋转 / 滚轮缩放
- 参数编辑：几何参数 + 属性（材料 / 等级 / 表面处理）实时编辑
- 属性导出：CSV / JSON

### 2. FreeCAD 建模（本次重构核心）
- **7 类紧固件**：螺栓、螺母、垫圈、螺钉、铆钉、销、螺柱
- 几何：六角头（正六棱柱拉伸）、内孔 / 内六角沉孔、球冠铆钉头、45° 倒角、螺纹示意（圆环堆叠）
- 布尔运算：`fuse`（并）/ `cut`（差）对应原版 UNION / SUBTRACT
- 输出格式：STEP（通用交换）、STL（3D 打印）、FCStd（FreeCAD 原生）
- 运行方式：`freecadcmd` 无界面，或 FreeCAD GUI 内 import

### 3. 标准库批量生成
- `generate-library.js` 读取 25 个样本 → 生成 `freecad_batch_model.py`
- 批量脚本按网格布局排布（避免重叠），一次性导出整套标准件
- 修复原版 CSV「标准号」取错字段导致空白的 bug

## 五、目录结构

```
AI+CAD-FreeCAD/
├── src/                          # Next.js 前端 + API
│   ├── app/page.tsx              # 三栏工作台
│   ├── app/api/
│   │   ├── parse-document/       # 文本 → 参数
│   │   ├── parse-pdf/            # PDF → 参数
│   │   ├── export-attributes/    # CSV/JSON 属性
│   │   └── export-freecad/       # FreeCAD 脚本 / 参数 JSON
│   ├── components/dashboard/     # 工作台组件
│   ├── components/three/         # three.js 参数化模型
│   └── lib/                      # 类型 + 参数生成
├── scripts/
│   ├── freecad_modeler.py        # ★ FreeCAD 建模（7 类）
│   ├── run_freecad.sh            # 定位 freecadcmd
│   ├── pdf_extract.py / mineru_pdf_parser.py
│   └── dev.sh / build.sh / ...
├── freecad_fastener_model.py     # ★ 蝶形螺母示例
└── standard-library/             # 25 样本 + 批量生成脚本
```

## 六、本地运行步骤

```bash
cd "/Users/kexinlv/Downloads/AI+CAD-FreeCAD"

# 1. 安装前端依赖（首次）
corepack enable
pnpm install

# 2. 启动 Web 服务
pnpm dev          # http://localhost:5000

# 3. 安装 FreeCAD（仅需一次）
brew install --cask freecad

# 4. FreeCAD 建模（推荐用 run_freecad.sh，自动定位 freecadcmd 并处理参数分隔）
./scripts/run_freecad.sh scripts/freecad_modeler.py -t bolt -d 10 -l 40
./scripts/run_freecad.sh freecad_fastener_model.py

# 5. 批量生成标准库
node standard-library/generate-library.js
./scripts/run_freecad.sh standard-library/output/freecad_batch_model.py

# 直接 freecadcmd 时需加 -- 分隔参数：
#   freecadcmd scripts/freecad_modeler.py -- -t bolt -d 10 -l 40
```

> 文档解析需在 `.env.local` 填入 `CUSTOM_LLM_API_KEY`（参考 `.env.example`）。

## 七、验证结果

| 项目 | 结果 |
|---|---|
| 7 类紧固件建模 | ✅ 全部实跑通过（bolt/nut/washer/rivet/pin 走批量库 25/25，screw/stud/蝶形螺母直接跑） |
| STEP / STL 输出 | ✅ 均正常生成（`output/*.step` + `*.stl`） |
| FCStd 输出 | ✅ `--fcstd` 正常保存 |
| 参数模板导出 | ✅ `--export-template` 无需 FreeCAD 即可运行 |
| `generate-library.js` | ✅ 生成 `freecad_batch_model.py` 并成功批量建模 |
| CSV 属性表 | ✅ 标准号 / 规格 / 材料字段正确填充 |
| TypeScript 类型检查 | ⚠️ 需 `pnpm install` 后运行 `pnpm ts-check` |

## 八、注意事项 / 待办

1. **FreeCAD 已实测通过**：本机已安装 FreeCAD 1.1.3，7 类紧固件 + 批量库 + 蝶形螺母均已实跑生成 STEP/STL，无报错；
2. **螺纹为装饰性示意**：用圆环（torus）堆叠近似，与原版环纹一致，真实牙型可按需细化；
3. **架构保持原版**：Web 端生成可下载脚本，用户在本地 `freecadcmd` 执行（不要求服务器装 FreeCAD）；
4. **未拷贝大文件**：`node_modules`（3.6G）、`.next`、`录屏.mp4`、`技术解决方案.pdf` 未复制，需 `pnpm install` 重新安装。

## 九、FreeCAD 踩坑记录（macOS 实测）

| # | 坑 | 现象 | 解决 |
|---|---|---|---|
| 1 | `freecadcmd` 不在 PATH | `command not found` | 实际位于 `Contents/Resources/bin/freecadcmd`，用 `run_freecad.sh` 自动定位 |
| 2 | 参数被 FreeCAD 抢走 | `unrecognised option '-d'` | `-d/-l/-t` 与 FreeCAD 自身选项撞车，需 `--` 分隔（`run_freecad.sh` 已内置） |
| 3 | `__name__` 不是 `__main__` | `main()` 不执行、无输出 | freecadcmd 以脚本文件名作为 `__name__`，脚本改为直接调用 `main()` |
| 4 | 螺旋扫掠做螺纹 | `Null shape` / 段错误 | `makePipe` 对螺旋线返回空几何，改圆环（torus）堆叠 |
| 5 | STL 导出 API 用错 | `TypeError: None of the objects...` | `Mesh.export` 只吃 Mesh 对象，改用 `MeshPart.meshFromShape` |
| 6 | 端口 5000 被占 | `pnpm dev` 报端口占用 | macOS「隔空播放接收器」默认监听 5000，需关闭或换端口 `DEPLOY_RUN_PORT=5001` |

